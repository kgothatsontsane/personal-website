services:
  - type: web
    name: personal-website
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: serve -s dist
    envVars:
      - key: NODE_VERSION
        value: 20
